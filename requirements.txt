# ====================================================================
#  DALAL TERMINAL - dependencies
#  Python 3.11+ recommended.  pip install -r requirements.txt
# ====================================================================

# --- Zerodha Kite Connect (live quotes, websocket, historical) -------
kiteconnect==5.2.0

# --- Numerics / data -------------------------------------------------
pandas>=2.1
numpy>=1.26
pyarrow>=14.0            # parquet on-disk cache

# --- Configuration ---------------------------------------------------
pydantic>=2.5
pydantic-settings>=2.1
python-dotenv>=1.0

# --- Web terminal (FastAPI + websockets) -----------------------------
fastapi>=0.110
uvicorn[standard]>=0.27
jinja2>=3.1
websockets>=12.0

# --- Fundamentals (Milestone 4) --------------------------------------
yfinance>=0.2.40
httpx>=0.27

# --- News / announcements (Milestone 5) ------------------------------
feedparser>=6.0
beautifulsoup4>=4.12

# --- Sentiment (Milestone 5) -----------------------------------------
#   Lexicon scorer needs nothing extra. FinBERT is optional & heavy:
# transformers>=4.40
# torch>=2.2

# --- ML calibration + backtesting (Milestone 6) ----------------------
scikit-learn>=1.4

# --- Kite daily-login helper -----------------------------------------
pyotp>=2.9

# --- Dev / tests -----------------------------------------------------
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27              # also used by FastAPI TestClient
